{
  "variants": [
    {
      "name": "create rows",
      "config": {
        "script": "create rows"
      }
    },
    {
      "name": "replace all rows",
      "config": {
        "script": "replace all rows"
      }
    },
    {
      "name": "partial update",
      "config": {
        "script": "partial update"
      }
    },
    {
      "name": "select row",
      "config": {
        "script": "select row"
      }
    },
    {
      "name": "swap rows",
      "config": {
        "script": "swap rows"
      }
    },
    {
      "name": "remove row",
      "config": {
        "script": "remove row"
      }
    },
    {
      "name": "create many rows",
      "config": {
        "script": "create many rows"
      }
    },
    {
      "name": "append rows to large table",
      "config": {
        "script": "append rows to large table"
      }
    },
    {
      "name": "clear rows",
      "config": {
        "script": "clear rows"
      }
    }
  ]
}
